name: Azure VM Deployment

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deployment:
    runs-on: ubuntu-latest

    environment:
      name: Azure-Production
      url: https://172.198.76.1/

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Track Azure deployment
        run: echo "MedicalRag is live on Azure VM at https://172.198.76.1/"
